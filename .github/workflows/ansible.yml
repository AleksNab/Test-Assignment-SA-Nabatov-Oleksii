name: Ansible
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lint
        uses: ansible/ansible-lint@main
        env:  
          ANSIBLE_ROLES_PATH: '/home/runner/work/Test-Assignment-SA-Nabatov-Oleksii/Test-Assignment-SA-Nabatov-Oleksii/ansible/roles'
           
